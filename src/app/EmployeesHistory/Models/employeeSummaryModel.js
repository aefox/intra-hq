class EmployeeSummaryModel {
  constructor(joinedDate, techExperience, hobbies, projectsHistory) {
    this.joinedDate = joinedDate;
    this.techExperience = techExperience;
    this.hobbies = hobbies;
    this.projectsHistory = projectsHistory;
  }
}

export default EmployeeSummaryModel;
